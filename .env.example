# Cloudflare Web Analytics beacon token (Dashboard → Web Analytics → Manage site).
# Required at `next build` / OpenNext build time for analytics to load in production.
NEXT_PUBLIC_CF_WEB_ANALYTICS_TOKEN=
